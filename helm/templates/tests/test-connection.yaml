apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konfirm.fullname" . }}-test-connection"
  labels:
    {{- include "konfirm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konfirm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
