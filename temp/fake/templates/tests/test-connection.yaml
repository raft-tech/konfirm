apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fake.fullname" . }}-test-connection"
  labels:
    {{- include "fake.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fake.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
